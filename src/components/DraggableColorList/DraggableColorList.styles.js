import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  colors: {
    height: '100%',
    display: 'flex',
    flexWrap: 'wrap',
    alignContent: 'start',
    overflowX: 'hidden'
  }
});

export default useStyles;