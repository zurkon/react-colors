import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    width: '20%',
    height: '25%',
    display: 'block',
    position: 'relative',
    cursor: 'pointer',
  }
});

export default useStyles;